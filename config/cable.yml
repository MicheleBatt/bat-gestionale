development:
  adapter: async

test:
  adapter: test

# L'applicazione non usa broadcast via Action Cable (nessun turbo_stream_from), quindi non serve
# Redis: l'adapter async lavora in-process ed evita una dipendenza in più da installare sul server.
production:
  adapter: async
