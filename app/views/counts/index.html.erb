<p style="color: green"><%= notice %></p>

<h4 class="d-flex d-lg-none m-4">Conti corrente</h4>

<div id="counts" class="d-block">
  <% @counts.each do | count | %>
    <%= render count %>
  <% end %>
</div>

<div class="d-flex justify-content-center justify-content-lg-end my-5 mx-4">
  <%= link_to "Nuovo conto corrente", new_count_path, class: "btn btn-success" %>
</div>
